#include <stdio.h>
#include <stdlib.h>
/**
 * main - main func
 * @void: default param
 * Return: always
 */
int main(void)
{
	/**
	 * while - loop
	 * @charr: variable
	 * Return: always 0
	 */
	int charr = 'a';
	
	while (char <= 'z')
	{
		putchar(charr);
		a++;
	}
	
	return (0);
}
