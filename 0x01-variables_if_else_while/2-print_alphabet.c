#include <stdio.h>
#include <stdlib.h>
/**
 * main - main func
 * @void: default param
 * Return: always
 */
int main(void)
{
	/**
	 * while - loop
	 * @charr: variable
	 * Return: always 0
	 */
	char charr = 'a';
	
	while (charr <= 'z')
	{
		putchar(charr);
		a++;
	}
	
	return (0);
}
